info:
  name: createATodo
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{graqphQLURl}}"
  body:
    query: |-
      mutation CreateTodo($input: CreateTodoInput!) {
        createTodo(input: $input) {
          id
          title
          deadline
        }
      }
    variables: '{ "input": { "title": "todo1", "description": "des of todo1", "deadline": "2026-07-01T12:00:00Z", "todoListId": 5 } }'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
