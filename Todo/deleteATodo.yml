info:
  name: deleteATodo
  type: graphql
  seq: 3

graphql:
  method: POST
  url: "{{graqphQLURl}}"
  body:
    query: |-
      mutation DeleteTodo($id: Int!) {
        deleteTodo(id: $id)
      }
    variables: '{ "id": 3 }'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
