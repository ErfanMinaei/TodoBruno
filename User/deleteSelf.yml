info:
  name: deleteSelf
  type: graphql
  seq: 5

graphql:
  method: POST
  url: "{{graqphQLURl}}"
  body:
    query: |-
      mutation DeleteSelf {
        deleteSelf
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
