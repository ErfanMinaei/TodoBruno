info:
  name: deleteTodolist
  type: graphql
  seq: 3

graphql:
  method: POST
  url: "{{graqphQLURl}}"
  body:
    query: |-
      mutation DeleteTodoList($id: Int!) {
        deleteTodoList(id: $id)
      }
    variables: '{ "id": 5 }'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
