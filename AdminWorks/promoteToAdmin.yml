info:
  name: promoteToAdmin
  type: graphql
  seq: 7

graphql:
  method: POST
  url: "{{graqphQLURl}}"
  body:
    query: |-
      mutation PromoteToAdmin($userId: Int!) {
        promoteToAdmin(userId: $userId) {
          id
          username
          roles
        }
      }
    variables: '{ "userId": 3 }'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
